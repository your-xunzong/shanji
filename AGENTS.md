# AGENTS.md

## 项目目标

本仓库用于开发“闪记”（暂定名）：一款离线优先、轻量、可移植的 Windows/macOS/Linux 快速备忘与提醒工具。

产品需求与业务语义以 `docs/PRD.md` 为当前事实来源。若实现与文档不一致，应先说明差异；产品行为变更必须同步更新 PRD，重要技术取舍记录为 ADR。

## 当前阶段

- 当前处于需求评审和技术验证阶段。
- 未经明确请求，不要提前加入账号、云同步、遥测、AI 云服务或团队协作功能。
- 优先验证全局快捷键、窗口聚焦、系统通知、休眠恢复、时间变化和持久化闭环。

## 产品不可破坏项

- 快速录入主链路必须保持：全局唤起、立即输入、一次提交、可靠落盘。
- 只有数据持久化成功后才能隐藏快速录入窗口；失败时必须保留原文并允许重试或复制。
- 未指定时间的事项遵循 PRD 中“工作日下班时间”和“下一工作日顺延”规则。
- 默认到期时间必须从持久化设置读取，不得写死为 18:00；设置变更默认只影响新事项和之后的自动顺延，不得静默批量修改已有到期时间。
- “今日必做”事项不得自动顺延；到期后即使已过下班时间也按后台配置间隔持续提醒，直到完成、明确暂停或取消该标记。
- 持续提醒间隔不得写死。调整间隔、休眠恢复、重启和系统时间回拨时必须重新计算下一次提醒，并通过幂等键避免补发风暴。
- 自动推断的时间或分类必须可见、可撤销，不得改写用户原文。
- 事项完成后不得继续提醒；休眠、重启、多开和系统时间回拨不得制造重复提醒。
- 默认离线工作。不得采集事项正文或全局键盘输入，不得在未授权时联网。
- MVP 仅注册明确配置的组合快捷键；不得用全局键盘记录模拟“双击单键”。

## 技术基线

- 桌面框架：Tauri 2；核心逻辑使用 Rust。
- UI：TypeScript + Svelte，优先原生 CSS 和少量可访问组件，避免大型 UI 依赖。
- 持久化：SQLite。Rust 数据访问层是唯一写入口，UI 不直接执行 SQL。
- Rust 领域层负责时间、工作日、顺延和提醒规则；UI 只展示结果和提交命令。
- 所有系统能力经适配器隔离：时钟/时区、通知、全局快捷键、窗口、托盘、开机启动和文件路径。
- 以上基线若需变更，先新增或更新 `docs/adr/` 下的 ADR，并说明体积、性能、维护性及三平台影响。

## 数据与时间规则

- SQLite 是事项和提醒计划的事实来源；内存定时器不得成为唯一状态。
- 审计时间使用 UTC。涉及“当地几点”的业务语义需同时保留本地时间意图和时区策略。
- 时间计算必须使用可注入的 `Clock`/时区抽象，不得在领域逻辑中散落直接读取系统当前时间的调用。
- 每次提醒必须有稳定幂等键；发送前原子领取或记录，避免重复通知。
- 数据库 schema 变更必须提供版本化迁移、迁移测试和失败恢复方案。
- 不得静默丢弃未知或损坏数据。导入前校验；升级迁移前创建可恢复备份。

## 跨平台约束

- 共享领域逻辑不得依赖平台条件编译；平台差异只放在适配器或启动装配层。
- 新增系统集成功能时，必须说明 Windows、macOS、Linux 的支持状态和降级行为。
- 不假定托盘、Wayland 全局快捷键、通知动作或开机启动在所有环境中行为相同。
- 窗口相关修改至少考虑多显示器、负坐标、混合 DPI、全屏应用和焦点归还。
- 键盘交互必须验证中文输入法组合态；不得把 IME 候选确认的 Enter 当作提交。
- 普通安装的数据目录与程序目录分离；便携模式只能由显式标记或设置启用。

## 性能与依赖预算

- 以 `docs/PRD.md` 的 P95 延迟、空闲 CPU、内存和包体积目标作为发布门槛。
- 不得为单一小功能引入大型运行时或常驻后台服务。
- 新增生产依赖前检查许可证、维护状态、二进制体积、系统权限和三平台支持，并在变更说明中写明理由。
- 空闲路径避免高频轮询；优先系统事件加低频健康检查。
- 不在快速录入保存路径执行网络请求、模型推理或非必要批处理。

## 安全与隐私

- 使用 Tauri capabilities 的最小权限集合；不要启用未使用的插件命令或宽泛文件系统访问。
- 快捷键功能只处理已注册快捷键事件，不记录其他按键。
- 日志、错误报告、测试快照和遥测不得包含事项正文、备注、剪贴板或凭据。
- 凭据使用操作系统安全存储，不写入 SQLite、配置文件、日志或版本库。
- 导出、永久删除、数据目录迁移和数据库修复属于高风险操作，必须有确认、错误恢复和可验证结果。

## 工程实现规则

- 优先小而可审查的改动，不做与当前需求无关的重构。
- 业务状态使用明确类型和枚举，避免散落字符串、魔法数字和隐式时区转换。
- 保存事项及其初始提醒/事件记录必须在同一事务中完成。
- 后台任务必须可取消，并在应用退出时有明确收尾；不得用无法追踪的无限循环。
- 用户可见错误应说明发生了什么、数据是否安全、下一步怎么做；技术细节写入去敏日志。
- 行为变更同步更新 PRD、用户文案和测试。新增架构决策时创建 ADR。

## 测试要求

- 领域规则优先单元测试，使用固定时钟覆盖：下班前后、今日必做的重复提醒与暂停、跨午夜、非工作日、月末、年末、闰日、夏令时、时区变化和时间回拨。
- 数据层测试覆盖事务回滚、重复提醒幂等、迁移、导入冲突、数据库只读/损坏和磁盘写入失败。
- UI 测试覆盖快捷键唤起、自动聚焦、`Enter`/`Shift+Enter`/`Esc`、IME 组合态、保存失败保留草稿和无障碍焦点。
- 平台集成测试覆盖 Windows、macOS、Linux；Linux 至少记录 Wayland 与 X11 结果。
- 修复缺陷时先添加能复现问题的测试；如无法自动化，记录稳定的手工复现与验证步骤。
- 完成代码改动后运行受影响范围的格式化、静态检查、单元测试和集成测试，并在交付说明中报告实际运行结果。

## 文档与提交

- 用户可见文档优先使用中文；代码标识符、协议字段和提交信息可使用英文，但同一范围保持一致。
- 注释解释“为什么”和边界条件，不复述代码。
- 不提交构建产物、日志、数据库、导出备份、凭据或包含用户内容的测试夹具。
- 在工具链尚未建立前，不虚构可运行的命令。脚手架完成后，应在本文件补充准确的构建、测试、检查和打包命令。

## 功能需求门禁与版本发布

- 任何新增功能或用户可见行为变更，必须先输出经过润色的 PRD 草案，写明目标、范围、非目标、失败行为、跨平台差异和验收标准。
- 只有用户明确确认该版 PRD 后，才能修改功能代码；确认前仅允许只读诊断、技术验证和文档草案，不得提前实现。
- 用户确认后，先同步 `docs/PRD.md`；技术路径有重要取舍时同步新增或更新 ADR，然后再进入实现。
- 缺陷修复若改变用户可见语义，同样适用 PRD 确认门禁；仅恢复已确认行为且无新增语义的内部修复可直接进入补丁流程，但仍需同步测试和版本日志。
- 采用语义化版本：不兼容变更提升主版本，向后兼容的新功能提升次版本，向后兼容的缺陷修复提升补丁版本。
- 每次功能发布必须同步更新 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` 及相关锁文件中的应用版本。
- 每次版本迭代必须更新根目录 `CHANGELOG.md`，按 `Added`、`Changed`、`Fixed`、`Security` 等分类记录用户可见变化、迁移要求和已知降级。
- 未完成真实检查和验收前，版本日志保持在 `Unreleased`；通过发布门槛后再写入版本号与发布日期。
- Git 提交应使需求文档、实现、测试和版本日志可追溯；不得将未经确认的功能混入已确认版本。

## 已验证命令

从仓库根目录运行：

- 安装依赖：`pnpm install`
- 前端开发：`pnpm dev`
- 前端类型检查：`pnpm check`
- 前端测试：`pnpm test`
- 前端生产构建：`pnpm build`
- 桌面开发：`pnpm tauri dev`
- Windows NSIS 构建：`pnpm tauri build --bundles nsis`
- Windows 便携包：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts/package-portable.ps1`

从 `src-tauri/` 目录运行：

- Rust 格式检查：`cargo fmt --all -- --check`
- Rust 静态检查：`cargo clippy --all-targets --all-features -- -D warnings -A linker_messages`
- Rust 测试：`cargo test`

Windows 命令需在 MSVC Developer PowerShell/Command Prompt 中运行，或预先加载 `vcvars64.bat`。`linker_messages` 仅用于忽略本地化 MSVC 链接器的正常标准输出，不得用于压制其他警告。

## 完成定义

一项功能只有在以下条件满足时才算完成：

1. 行为符合 PRD 和对应验收标准。
2. 三平台影响与降级路径已说明。
3. 核心成功、失败、恢复和时间边界已有测试。
4. 无新增未说明的联网、权限、敏感日志或数据迁移风险。
5. 相关文档已同步，且实际执行过适用的检查与测试。
