# 更新日志

本项目采用语义化版本。未完成验证的变化先记录在 `Unreleased`，通过发布门槛后再写入版本号和发布日期。

## [Unreleased]

## [0.13.2] - 2026-09-09

### Changed

- 应用版本提升到 v0.13.2；SQLite schema、提醒计划、数据格式、联网权限和跨平台标准通知行为保持不变。

### Fixed

- 修复 Windows 原生持续通知在系统限定宽度或显示缩放下第三个操作被裁切的问题：操作行保留完整可见的“完成”“15 分钟后”，点击通知正文继续打开对应事项。

## [0.13.1] - 2026-09-08

### Added

- 新增用户授权后的 GitHub 稳定版更新检查：启动 30 秒后按 24 小时限流检查，也可在“关于闪记”或托盘中手动检查；默认保持离线。
- 新增签名更新抽屉，展示版本轨、3–6 条中文更新说明、真实下载进度、取消、稍后提醒与安装重启状态；更新后首次启动只展示一次同源说明。
- 发布流水线生成 Windows、macOS、Linux 更新包签名与 `latest.json`，先创建待维护者确认的稳定版草稿，正式发布后客户端才可发现。
- 新增启动前本机数据检查页。当前目标为空但发现经过校验的旧数据时，不再进入空事项列表，可核对记录数、提醒历史和更新时间后恢复原数据。
- 新增个人数据仓库。用户可选择目录、预览来源并同步经过校验的跨平台 `.sjpack` 数据包；运行中的 SQLite 继续留在可靠的本机数据目录。
- 新增保守的数据归并闭环：相同修订幂等跳过，完成与删除墓碑参与同步，两侧均有变化时保留完整冲突副本，重复同步不会反复制造同一冲突。
- 新增邮件提醒工作台，可按事件类型、类型或标签建立“首次到期、每天第一次、每个计划时点、每日摘要”规则，并查看连接验证和最近投递状态。
- 邮件同时生成纯文本和无远程资源的内联 HTML；每日摘要使用适合常见邮件客户端的表格布局。
- 新增只读事项甘特图，支持日、周、月、年范围、今日线和组合筛选；v0.11.0 区间返工后，所有事项以创建至截止/今天的横条呈现，活动/预警在条内标记，周期事项同一行分段。
- v0.13.1 甘特图在同一生命周期条内加入已记录、进行中、已完成和逾期色段；未完成逾期从原截止以红色斜纹延伸到今天，并保留原截止刻痕。

### Changed

- 应用版本提升到 v0.13.1；SQLite schema 保持 v11，本版不新增数据迁移、生产依赖或联网权限。
- 个人仓库选择完成后立即写入本机快照，主操作明确为“检查其他设备的数据”；汇入完成后刷新事项、类型、标签和待确认提醒，并可直接查看全部事项。
- 中文界面统一优先使用微软雅黑 UI、苹方或 Noto Sans CJK，正文基线提升到 14px，按钮/输入不小于 13px，常规辅助文字不小于 12px；中文标题不再优先使用 Bahnschrift。
- v0.11.1 将提醒设置拆为“提醒显示”和“错过提醒”：系统持续提醒与屏幕角落提醒窗只决定提醒从哪里出现，文案明确不会改变频率；待确认提醒显示真实数量并可直接打开。
- 移除与逐事项提醒方案重复的“普通事项未确认时重复提醒”全局开关。旧数据库字段继续兼容读取但不再参与调度，保存设置时自动关闭旧值；普通、今日必做及其他事件均按自身已保存方案计算下一次提醒。
- v0.11.0 按已确认的验收返工 PRD 重做主工作台与甘特图：紧凑导航、固定事项列/日期表头、连续网格、可见行渲染；日视图由后端按真实小时投影，夏令时保留 23/25 小时和重复小时标识。
- 截止包含已保存的默认值与自动顺延结果；无截止显示至今天，逾期与完成保留原定区间。旧周期没有完整历史时标注“历史未记录”，不从提醒时间反推历史；异常记录进入可打开的“时间待核对”。本次不新增数据库迁移、依赖、联网或权限。
- 账户/云空间与多设备同步改为后续独立排期，不包含在 v0.11.0 中。
- 将后续大范围需求拆为独立的 `0.x` 版本：v0.7.1 先修复更新与重装的数据保留，之后分别完成本地数据归并、邮件提醒体系、只读甘特图、账户云空间和多设备同步。上述能力均不提前占用 v1.0.0。
- v1.0.0 暂不排期；只有核心功能稳定、升级数据可靠且 Windows、macOS、Linux 达到正式发布门槛后，才单独评审是否进入正式版。
- 确认 v0.7.1 数据保留补丁范围：覆盖更新、重装和安装路径变化不得读取为空数据；迁移前创建一致性备份，失败时保留原库且不以新空库继续运行。
- 确认账户与云空间基线：未登录仅使用本地数据；登录后启用云端操作；免费空间初始为 100 MB；账户注销提供 30 天撤销期。
- 新增 UI 设计门禁：新界面或明显重构必须使用 `frontend-design` skill 完成两轮设计、自检和截图复核，并沿用闪记的时间轨视觉语言。
- schema 升级改为先创建一致性备份，再在临时副本完成迁移和校验；切换前保留原数据库及 WAL/SHM，失败时不改写原库。
- SQLite schema 升级到 v10，加入仓库实例/修订/墓碑/冲突、邮件规则/摘要队列/投递状态和时间画布偏好；旧版标签邮件路由迁移为等价规则，但不会伪造连接测试成功。
- SMTP 服务器身份、用户名或密码改变后必须重新发送测试邮件；测试成功前规则可以保存但后台不会联网投递。
- 事项标签从逐项读取改为批量加载；时间画布只读取投影所需字段，并限制为 2000 个可见节点和 200 条待排期事项。

### Fixed

- 修复个人仓库刚选择位置但未写入本机数据包、归并后当前筛选之外的数据看似没有汇入的问题；快照失败现在与本机汇入结果分别显示并可单独重试。
- 修复屏幕角落提醒窗先清空内容再隐藏、隐藏失败后无法关闭的问题；关闭按钮在正常与空载荷状态始终可用，支持 `Esc` 且不改变事项状态或提醒计划。
- v0.11.1 删除会错误创建今日必做事项的“普通事项重复测试”和会创建真实记录的“提醒中心测试”；提醒中心改为打开现有事实列表，避免把展示渠道、提醒频率和找回入口误认为四套重复功能。
- 甘特图切换月份不再因月末跳过二月；较早请求不会覆盖较新结果，更新失败保留旧图和真实日期标题，首次失败可返回列表；键盘支持逐项、首尾和横向导航，组合态不触发导航。
- v0.10.2 修复验收 A1“选择仓库位置”点击无反应：仅主窗口获得原生文件夹选择权限；选择器异常可见且可重新选择，取消不写配置，选择期间防止重复打开。本机数据目录与归并规则不变，甘特图区间及视觉返工另行确认。
- v0.10.1 修复 Windows 安装与卸载向导仍显示英文的问题：固定简体中文，补齐重装、运行组件失败及删除数据提示；不改变安装权限、数据位置和卸载默认保留数据的行为。
- 修复覆盖更新、相同版本重装或安装目录变化后，目标路径异常时 SQLite 先创建空库并把用户带入空列表的问题。
- 修复恢复切换时直接删除旧 WAL/SHM 的风险；原文件现在随数据库一起进入可恢复备份。
- 修复同一来源分歧数据包再次归并时重复创建冲突副本的问题；已见修订现在记录并复用本机目标事项。
- 修复应用错过每日摘要时必须等到下一个摘要时点的问题；前一日队列会在重启后补领，发送中断最多恢复三次后进入需要处理。
- 修复同一天摘要已提交后新增事项错误并入已发送摘要的问题；后续事项排入下一次摘要，删除规则会停止没有队列内容的遗留投递。
- 修复时间画布在今天不属于当前视窗时仍把今日线贴在边缘的问题，并补全收起侧栏图标的可访问名称。

### Security

- 更新包强制使用仓库外长期私钥签名，公钥随应用发布；客户端只接受官方仓库 Release URL，签名或来源不通过时拒绝安装且不改动当前数据。
- 更新检查不发送事项、备注、类型、标签、本机路径或 SMTP 信息；远程更新说明只按转义后的纯文本列表展示。
- 启动恢复诊断只包含文件状态、数据版本和候选数量，不记录事项正文、备注、草稿内容或 SMTP 凭据。
- 仓库数据包不包含绝对路径、系统凭据、开机启动状态或验证状态；导入前校验版本、表白名单和 SHA-256，失败时不写入运行库。
- 邮件 HTML 对事项文字做转义，不加载远程图片、字体或跟踪像素；投递日志只保存哈希化收件标识、状态和去敏错误码。

### Platform

- Windows v0.13.1 x64 NSIS 已生成独立更新签名并通过 27 条简体中文安装/卸载文案检查；安装包 4,848,160 字节，便携 ZIP 6,529,072 字节且只含程序与 `portable.flag`，文件/产品版本均为 0.13.1。macOS/Linux 的角落窗关闭、同步盘原子写入及应用内升级仍待实机验收。
- Windows v0.12.0 x64 NSIS 已生成独立更新签名并通过简体中文安装脚本检查，便携 ZIP 已核对仅含程序与 `portable.flag`；文件/产品版本均为 0.12.0。更新签名不等同于 Windows 发布者代码签名，SmartScreen 仍可能显示“未知发布者”；从 v0.12.0 到后续版本的完整更新闭环及 macOS/Linux 实机仍待验收。
- Windows v0.11.1 x64 安装包与便携 ZIP 已构建，版本、中文安装脚本及 SHA-256 校验通过，当前未配置发布者签名。浏览器已复核提醒设置的浅色、深色、高对比度与强制颜色样式；Windows 原生持续通知、置顶窗焦点和托盘数量仍待安装环境统一验收。
- Windows v0.11.0 x64 安装包与便携 ZIP 已构建并核对版本及 SHA-256，包含 A1 修复与简体中文安装向导，保持未签名验收包状态。前端 56 项、Rust 87 项测试通过（另 1 项默认忽略）；安装环境 A1、真实输入法、系统强制颜色、完整桌面性能和 macOS/Linux 实机仍待验收，本次不宣称正式发布完成。
- Windows v0.10.2 x64 安装包与便携 ZIP 已构建，延续简体中文安装向导及未签名状态；A1 已补桌面分支和权限回归，仍需安装环境复验，不代表其他仓库能力已通过验收。
- Windows v0.10.1 x64 NSIS 安装包和便携 ZIP 已构建，安装/卸载向导固定简体中文，当前未配置发布者签名；macOS/Linux 分发行为不变，系统凭据、SMTP 实投及字体/滚动仍待真实设备验收。

## [0.7.0] - 2026-09-01

### Added

- “重复”方案新增每天两个默认提醒时点，初始为 `10:00` 和 `17:00`；后台和首次引导均可设置。
- 事项编辑器可在“使用当前两个默认时间”和“每天一个指定时间”之间切换，并明确复制当前默认值。

### Changed

- SQLite schema 升级到 v7。每个重复事项保存自己的当地时间快照；修改后台默认值不会静默改动已有事项。
- 首次引导版本提升，v0.6.x 用户升级后会看到一次新版时间设置；跳过或关闭不会修改原设置。
- 重复事项从休眠或退出恢复时最多补发一条，随后直接进入下一个未来时点；同一日期、事项和时点继续幂等。
- 只读甘特图顺延到 v0.8.0。

### Fixed

- 重复事项的通知时间改为显示实际触发时点，第二次提醒不再沿用第一次的时间文字。
- 预警事件到达目标时间并完成最后一次投递后停止，不再越过目标时间继续重复。
- 修复 Windows 专用通知操作在 macOS/Linux 严格检查中被误判为未使用，恢复三平台发布流水线。
- 修复 Windows runner 解码中文产品名后无法汇集已生成安装包的问题，发布资产统一使用 ASCII 文件名。

### Platform

- Windows、macOS、Linux 共用同一双时点计算和 schema 迁移；macOS/Linux 通知、快捷键和分发仍需真实设备验收。

## [0.6.0] - 2026-09-01

### Added

- 新增普通、一次性、今日必做、预警、持续、月度和年度七种固定事件类型，“重要”可独立标记任意事件。
- 新增重复、强调、一次性、强制和自定义五种提醒方案，并可为七种事件分别设置默认方案。
- 新增待选择事件类型的单条操作通知和多条合并通知；支持设为普通、选择其他事件类型或稍后处理。
- 后台新增“关于闪记”，从运行时读取版本，展示版权、离线与隐私说明、数据位置和开源许可入口。
- Excel 新增事件类型、单选类型、标签、重要标记、提醒方案、起止时间和预警目标字段，并分别生成事件类型与类型汇总。
- 新增 Windows、macOS、Linux 的可复现测试构建流水线：Windows x64 生成 NSIS 与便携 ZIP，macOS 生成 Apple Silicon/Intel 通用 DMG，Linux x64 生成 AppImage 与 DEB。
- 跨平台测试发布附带 SHA-256 校验文件，并默认保持为草稿预发布，等待真实设备验收。

### Changed

- 事项整理升级为“事件类型—类型—标签”三层模型：事件类型表达时间性质，工作/个人等继续作为自定义单选类型，项目/客户等作为彩色多选标签。
- 新建且尚未选择事件类型的记录默认每天提醒；快速录入仍只需输入内容并按 Enter，精细类型留在后台编辑。
- SQLite schema 升级到 v6。升级前创建一致备份；v5 类型和标签及其关系分别原样保留，不互相复制。
- 逾期事项保留原到期时间；转换事件类型时可从当前建立新的未来计划。
- 便携包脚本默认从应用配置读取版本，避免版本升级后错误沿用旧文件名。

### Fixed

- 一次性事件在其本地日期结束后不再补发；“今日必做 + 一次性”也会在次日停止。
- 持续事件超过结束时间后停止，休眠错过多个周期时只补一条并跳到下一个有效时间。
- 月度事件的月末日期和年度事件的闰日按当地日历使用目标月最后一个有效日期。
- 月度和年度事件使用重复、强调、强制或自定义方案时会持续跟进当前周期，完成本次后再进入下一周期，不会提前跳期。

### Security

- 待选择事件类型的合并通知不包含事项正文；迁移、日志和导出继续遵循离线与敏感内容边界。

### Platform

- macOS 在未配置 Apple Developer 凭据时使用临时签名，仅作为测试包；Windows 当前也未配置发布者证书。
- macOS 和 Linux 的快捷键、托盘、通知、休眠恢复与开机启动仍需真实设备验证，通过前不得标为正式支持。

## [0.5.0] - 2026-08-29

### Added

- 新增待确认提醒中心：保存所有已经到期但尚未明确处理的事项，并在托盘菜单同步显示数量。
- 新增可选的应用置顶提醒窗，支持多事项排队、完成、15 分钟后提醒和打开闪记；窗口默认关闭且不主动抢焦点。
- Windows“今日必做”优先使用原生持续提醒，并提供完成、稍后提醒和打开操作；不支持时自动降级为标准通知。
- 普通事项可开启“未确认时重复提醒”，间隔可在后台设置；明确确认后停止该事项的普通重复提醒。
- 新增默认关闭的 SMTP 邮件渠道，可按彩色标签选择发送范围，并提供真实连接及测试邮件入口。
- 后台为标准通知、原生持续提醒、置顶提醒窗、重复提醒和提醒中心分别提供测试入口。

### Changed

- SQLite schema 升级到 v5，新增提醒确认状态、渠道投递记录和标签邮件路由；升级前自动创建一致备份。
- 系统通知关闭或失败不再阻塞已启用的置顶提醒和邮件渠道；各渠道独立领取、记录和恢复。
- 邮件默认每个事项只在首次到期发送一次；可单独允许“今日必做”每次重复提醒都发邮件，并明确提示邮件量风险。

### Security

- SMTP 仅支持 TLS 或 STARTTLS；密码/授权码只写入操作系统凭据存储，不进入 SQLite、日志、Excel 或版本库。
- 邮件仅在用户明确启用且事项命中已选标签时联网；失败按 1 分钟、5 分钟有限重试，总尝试次数不超过 3 次。

### Platform

- Windows 已接入原生持续提醒、Credential Manager、置顶提醒窗和托盘待确认数量。
- macOS 的通知持续样式由系统设置决定；Linux 通知服务可能忽略持续提示。两者均保留置顶提醒窗降级路径，真实设备验证仍待完成。

## [0.4.0] - 2026-08-28

### Added

- 已有事项可完整编辑标题、备注、类型、多个彩色标签、提醒时间和“今日必做”规则。
- 类型与标签支持新增、重命名、改色、排序和安全删除；删除在用类型时必须先选择迁移目标。
- 删除事项先进入保留 30 天的回收站并停止提醒，支持短时撤销、恢复和二次确认后永久删除。
- Excel 报表支持按创建日期、状态、类型和标签筛选，生成“事项明细”和“分类汇总”工作表。
- Windows/macOS 托盘双击尝试打开主窗口；不支持托盘双击的环境继续提供“打开闪记”菜单。

### Changed

- SQLite schema 升级到 v4；升级前自动创建一致备份，已有类型和事项关系原样保留。
- 快速录入可直接选择多个标签，类型与“今日必做”继续保持独立语义。

### Fixed

- 旧预览数据缺少标签字段时，事项列表不再停留在加载状态。
- 快速录入的时间面板与标签面板不再相互覆盖。
- Excel 会转义以 `=`, `+`, `-`, `@` 开头的用户文字，避免被办公软件当作公式执行。

## [0.3.1] - 2026-08-28

### Added

- 快速录入新增明确的时间选择面板，可使用默认规则、今天下班、下一工作日或指定日期时间。
- 后台设置新增数据位置、记录数量、最近备份、手动一致备份和旧数据恢复入口。
- 数据恢复会先备份当前库，再在应用重启且数据库尚未打开时切换，避免运行中覆盖。

### Fixed

- 修复快速录入时间按钮由透明控件覆盖、点击后缺少可见反馈的问题。
- 升级与便携场景不再只复制 SQLite 主文件；升级前备份包含 WAL 中已经提交的数据。

## [0.3.0] - 2026-08-28

### Added

- 首次启动引导新增默认提醒时间设置，提供常用时间快捷选项；跳过不会修改原设置，完成后只影响新事项及之后的自动顺延。

## [0.2.1] - 2026-08-28

### Fixed

- 启动期间页面早于 Tauri 托管状态初始化时出现 `state not managed`、必须手动重试的问题。

## [0.2.0] - 2026-08-28

### Added

- 一次性的 v0.2.0 首次启动引导，可确认通知、开机启动和全局快捷键，并可从后台设置重新打开。
- 后台设置新增开机启动开关，展示并验证操作系统实际状态。

### Fixed

- 快捷键格式错误、只有修饰键、冲突或注册失败时显示具体原因，不再统一显示“设置没有保存”。

## [0.1.1] - 2026-08-28

### Added

- 后台设置显示通知身份与最近一次投递状态，便携版可明确启用或撤销当前用户级通知身份。
- 点击 Windows 通知可唤起主窗口并定位对应事项。

### Fixed

- Windows 原生系统通知身份、真实提交结果和失败提示。
- 便携版缺少 AppUserModelID 时测试事项显示“已排队”但没有系统横幅的问题。

### Changed

- 提醒事件区分已领取、已提交和失败，不再把入队状态当作系统通知成功。
- 提交失败按 1、5、15 分钟退避重试；启动后可恢复被中断的已领取提醒。
- Windows 便携版启用系统通知前需要用户明确授权，并提供撤销注册能力。
