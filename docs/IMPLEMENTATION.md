# MVP 实现状态

> 更新日期：2026-09-01

## 已完成闭环

| 能力 | 状态 | 说明 |
| --- | --- | --- |
| 快速录入 | 已实现 | 独立置前窗口、自动聚焦、Enter 保存、Shift+Enter 换行、Esc 隐藏、草稿恢复 |
| 全局快捷键 | Windows 已验证启动 | 默认 `Ctrl/Cmd + Shift + Space`，冲突时应用继续启动并提示后台换键 |
| 本地持久化 | 已实现 | SQLite + WAL，Rust 单一写入口，事项/初始事件/草稿清理同事务 |
| 默认到期 | 已实现 | 后台配置工作日与默认时间，下班后/非工作日创建转下一工作日 |
| 自动顺延 | 已实现 | 普通默认事项跨日后顺延；保留次数和事件；今日必做不顺延 |
| 今日必做 | 已实现 | 到期后按 5–240 分钟间隔重复，支持勿扰、暂停、恢复和完成停止 |
| 时间调整 | 已实现 | 未逾期事项可改时间；逾期事项保留原到期时间，转换事件类型时可从当前建立新的未来计划 |
| 三层整理 | 已实现 | 七种固定事件类型表达时间性质；工作/个人等类型自定义单选；彩色标签自定义多选，三层关系独立保存 |
| 事件与提醒方案 | 已实现 | 七种事件可切换，支持重复、强调、一次性、强制和自定义方案；七种事件的默认方案可在后台分别设置 |
| 事项编辑/回收站 | 已实现 | 完整编辑、完成/恢复、软删除与短时撤销、30 天回收站和永久删除确认 |
| Excel 导出 | 已实现 | 按日期、状态、事件类型、类型和标签筛选；生成事件类型/类型双汇总并防止公式注入 |
| 提醒幂等 | 已实现 | 事项 ID + 计划时间 + revision；同一到期周期不重复发送 |
| 系统通知 | Windows 运行路径已接入 | 后台设置与 `--test-notification` 可创建约 10 秒到期的真实测试事项；macOS 需补授权引导 |
| 持续提醒 | Windows 已接入 | 今日必做优先使用原生 Reminder 场景；可选置顶提醒窗不主动抢焦点并支持多事项排队 |
| 提醒中心 | 已实现 | 待确认事项与托盘数量来自 SQLite；完成、暂停、稍后提醒或明确确认后同步移除 |
| SMTP 邮件 | 已实现、默认关闭 | TLS/STARTTLS、按标签路由、首次到期默认只发一次、1/5 分钟有限重试；密码只进入系统凭据存储 |
| 托盘/单实例 | Windows 已接入 | 关闭窗口隐藏；托盘可快速记录、打开、退出；双击托盘图标打开主窗口 |
| 后台设置 | 已实现 | 默认时间、工作日、事件默认方案、间隔、勿扰、快捷键、五种提醒测试、邮件渠道与关于页 |
| Windows 分发 | 已实现 | NSIS 安装程序与带 `portable.flag` 的便携 ZIP |

## 自动验证

- 前端：`svelte-check` 零错误；Vitest 27 项通过。
- Rust：单元/数据层测试 48 项通过，另有 1 项手工 Excel 渲染用例默认忽略。
- Rust：Clippy 全目标、全 feature、警告视为错误时通过；仅允许本地化 MSVC 的 `linker_messages`。
- 生产前端：构建成功，v0.6.0 主 JS gzip 约 52 KB。
- Windows：Debug 桌面程序真实启动，SQLite 文件创建成功；观察到约 36 MB 工作集。
- 通知演示：测试事项经 SQLite 落盘，调度器登记一次性延迟唤醒，并沿用正式提醒的幂等处理路径。
- 今日必做：无显式时间且默认时间已过时立即到期；启动调度时自动修正旧版本误排到未来工作日的未完成记录。
- Excel：应用实际生成的明细与汇总工作表已通过结构检查、公式错误扫描和图片渲染检查。
- Windows 覆盖升级：已从已安装 v0.3.1/schema 3 升级到 v0.4.0/schema 4；3 条原有待办的匿名字段摘要升级前后一致，并生成 `before-v4` 一致备份。
- Windows 覆盖升级：已从已安装 v0.4.0/schema 4 升级到 v0.5.0/schema 5；同一批 3 条事项的非正文业务字段摘要升级前后一致，并生成 `schema-4-before-v5` 一致备份。
- v0.5.0 数据规则：提醒确认、普通事项重复停止、置顶渠道幂等、SMTP 标签路由、有限重试和密码不落库均有自动测试。
- v0.5.0 界面：后台提醒/SMTP 设置、待确认提醒中心和置顶提醒窗已通过应用内浏览器的语义、截图与控制台走查；无运行错误。
- v0.6.0 迁移：自动测试覆盖 schema 5 升级到 schema 6，原类型与标签分别保留且不互相复制；升级前备份路径已验证。
- v0.6.0 时间规则：自动测试覆盖一次性跨日停止、今日必做方案组合、预警、持续周期、强调次数上限、月末与闰日，以及月度“完成本次”不跳期。
- v0.6.0 界面：快速录入与后台三层整理界面已通过应用内浏览器走查；660×316 快速录入窗口可完整显示保存操作。
- Windows：v0.6.0 x64 NSIS 安装包构建成功；当前未配置发布者签名。

## 尚未完成

- macOS/Linux 真实设备的快捷键、托盘、通知、休眠恢复和分发验证。
- 系统通知权限的完整引导与拒绝后的恢复入口。
- 全文搜索、批量操作、完整 JSON 导入导出。
- 系统休眠/时区变化的事件驱动监听；当前由持久计划和 30 秒低频健康检查恢复。
- Windows 安装包签名、macOS 签名与公证。
- 月度、年度和平常事件的甘特图只读视图，计划进入 v0.7.0。
- 性能基线设备上的 P95 自动采样和 10 万条数据压力测试。
