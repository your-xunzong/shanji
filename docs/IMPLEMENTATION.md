# MVP 实现状态

> 更新日期：2026-09-08

## 已完成闭环

| 能力 | 状态 | 说明 |
| --- | --- | --- |
| 快速录入 | 已实现 | 独立置前窗口、自动聚焦、Enter 保存、Shift+Enter 换行、Esc 隐藏、草稿恢复 |
| 全局快捷键 | Windows 已验证启动 | 默认 `Ctrl/Cmd + Shift + Space`，冲突时应用继续启动并提示后台换键 |
| 本地持久化 | 已实现 | SQLite + WAL，Rust 单一写入口，事项/初始事件/草稿清理同事务 |
| 默认到期 | 已实现 | 后台配置工作日与默认时间，下班后/非工作日创建转下一工作日 |
| 自动顺延 | 已实现 | 普通默认事项跨日后顺延；保留次数和事件；今日必做不顺延 |
| 今日必做 | 已实现 | 到期后按 5–240 分钟间隔重复，支持勿扰、暂停、恢复和完成停止 |
| 时间调整 | 已实现 | 未逾期事项可改时间；逾期事项保留原到期时间，转换事件类型时可从当前建立新的未来计划 |
| 三层整理 | 已实现 | 七种固定事件类型表达时间性质；工作/个人等类型自定义单选；彩色标签自定义多选，三层关系独立保存 |
| 事件与提醒方案 | 已实现 | 七种事件可切换，支持重复、强调、一次性、强制和自定义方案；七种事件的默认方案可在后台分别设置 |
| 每日双时点重复 | 已实现 | 重复事项可复制后台两个默认时间或每天使用一个指定时间；事项保存快照，默认值变化不改旧事项 |
| 事项编辑/回收站 | 已实现 | 完整编辑、完成/恢复、软删除与短时撤销、30 天回收站和永久删除确认 |
| Excel 导出 | 已实现 | 按日期、状态、事件类型、类型和标签筛选；生成事件类型/类型双汇总并防止公式注入 |
| 提醒幂等 | 已实现 | 事项 ID + 计划时间 + revision；同一到期周期不重复发送 |
| 系统通知 | Windows 运行路径已接入 | 后台设置与 `--test-notification` 可创建约 10 秒到期的真实测试事项；macOS 需补授权引导 |
| 持续提醒 | Windows 已接入 | 今日必做优先使用原生 Reminder 场景；可选置顶提醒窗不主动抢焦点并支持多事项排队 |
| 提醒中心 | 已实现 | 待确认事项与托盘数量来自 SQLite；完成、暂停、稍后提醒或明确确认后同步移除 |
| SMTP 邮件 | 已实现、默认关闭 | TLS/STARTTLS；按事件类型、类型或标签建立首次/每日/每计划/摘要规则；连接测试通过后才允许后台发送，密码只进入系统凭据存储 |
| 托盘/单实例 | Windows 已接入 | 关闭窗口隐藏；托盘可快速记录、打开、退出；双击托盘图标打开主窗口 |
| 后台设置 | 已实现 | 默认到期、重复双时点、工作日、事件默认方案、间隔、勿扰、快捷键、提醒测试、邮件渠道与关于页 |
| Windows 分发 | 已实现 | v0.10.1 补齐 NSIS 安装/卸载向导简体中文；提供安装程序与带 `portable.flag` 的便携 ZIP，当前未配置发布者签名 |
| 更新数据保护 | 已实现 | 写连接前只读检查目标与旧位置；空目标不会覆盖非空旧库，恢复与迁移均先校验副本并保留原数据库及 WAL/SHM |
| 个人数据仓库 | v0.13.1 已补齐闭环，待统一验收 | 选择位置立即原子写入本机快照；`.sjpack` 校验、预览、备份、事务归并、墓碑与冲突副本可恢复；汇入后完整刷新相关视图 |
| 邮件投递规则 | 已实现 | 条件、策略、收件地址和 SMTP 连接分层；相同地址去重、摘要跨日恢复、有限重试和稳定幂等均由 SQLite 持久化 |
| 只读事项甘特图 | v0.13.1 已增强，待统一验收 | 创建至截止、完成或今天的连续状态段；原截止刻痕、完成刻点、逾期斜纹尾段、周期同一行分段及可见行渲染 |
| 签名更新 | v0.12.0 已实现，待跨版本验收 | 用户授权后检查官方 GitHub Release；安装版验证签名后下载、安装和重启，便携版与不支持原地更新的包只打开官方下载页 |
| 中文字体可读性 | v0.12.0 已实现，待多平台实机验收 | 中文平台字体栈、14px 正文基线、12px 常规辅助文字下限；更新授权条、设置卡和更新说明抽屉已完成真实内容渲染复核 |

## 自动验证

- 前端：`svelte-check` 零错误、零警告；Vitest 71 项通过，其中包含仓库首次快照/完整刷新、甘特图状态段与提醒窗隐藏失败恢复。
- Rust：单元/数据层测试 92 项通过，另有 1 项手工 Excel 渲染用例默认忽略。
- Rust：Clippy 全目标、全 feature、警告视为错误时通过；仅允许本地化 MSVC 的 `linker_messages`。
- 生产前端：桌面发行构建成功，v0.10.0 桌面主 JS gzip 约 64 KB。
- Windows：Debug 桌面程序真实启动，SQLite 文件创建成功；观察到约 36 MB 工作集。
- 通知演示：测试事项经 SQLite 落盘，调度器登记一次性延迟唤醒，并沿用正式提醒的幂等处理路径。
- 今日必做：无显式时间且默认时间已过时立即到期；启动调度时自动修正旧版本误排到未来工作日的未完成记录。
- Excel：应用实际生成的明细与汇总工作表已通过结构检查、公式错误扫描和图片渲染检查。
- Windows 覆盖升级：已从已安装 v0.3.1/schema 3 升级到 v0.4.0/schema 4；3 条原有待办的匿名字段摘要升级前后一致，并生成 `before-v4` 一致备份。
- Windows 覆盖升级：已从已安装 v0.4.0/schema 4 升级到 v0.5.0/schema 5；同一批 3 条事项的非正文业务字段摘要升级前后一致，并生成 `schema-4-before-v5` 一致备份。
- v0.5.0 数据规则：提醒确认、普通事项重复停止、置顶渠道幂等、SMTP 标签路由、有限重试和密码不落库均有自动测试。
- v0.5.0 界面：后台提醒/SMTP 设置、待确认提醒中心和置顶提醒窗已通过应用内浏览器的语义、截图与控制台走查；无运行错误。
- v0.6.0 迁移：自动测试覆盖 schema 5 升级到 schema 6，原类型与标签分别保留且不互相复制；升级前备份路径已验证。
- v0.6.0 时间规则：自动测试覆盖一次性跨日停止、今日必做方案组合、预警、持续周期、强调次数上限、月末与闰日，以及月度“完成本次”不跳期。
- v0.6.0 界面：快速录入与后台三层整理界面已通过应用内浏览器走查；660×316 快速录入窗口可完整显示保存操作。
- Windows：v0.6.0 x64 NSIS 安装包构建成功；当前未配置发布者签名。
- v0.7.0 迁移：自动测试覆盖 schema 6 升级到 schema 7；旧重复事项保持每天一个指定时间，已有正文、类型、标签和提醒历史不变。
- v0.7.0 时间规则：自动测试覆盖每日双时点、同日下一时点、最多补发一条、事项时间快照、恢复当前默认以及预警目标后停止。
- v0.7.0 界面：后台设置、首次引导和事项编辑器已通过语义、截图与控制台走查；窄窗口下引导内容可滚动，底部操作始终可见。
- Windows：v0.7.0 x64 NSIS 安装包构建成功；当前未配置发布者签名。
- v0.7.1 启动保护：自动测试覆盖目标缺失、空目标发现旧数据、损坏库、未来 schema、安装路径变化和恢复重启边界；阻塞时不创建快捷录入、提醒或调度运行路径。
- v0.7.1 界面：恢复候选与无安全候选两种状态已通过 760×560 最小窗口语义、截图、键盘焦点和控制台复核；无横向溢出，诊断默认收起且不含事项正文。
- Windows：v0.7.1 x64 NSIS 安装包与便携 ZIP 构建成功；可执行文件产品版本为 0.7.1，当前未配置发布者签名。
- v0.8.0 仓库：自动测试覆盖数据包往返、摘要篡改拦截、分歧冲突副本和重复归并不再制造第二份冲突；迁移前备份与事务回滚保持原运行库可恢复。
- v0.9.0 邮件：自动测试覆盖相同地址多规则去重、每日首次、错过摘要后的跨日恢复、摘要中断三次后停止，以及 HTML 转义、无远程资源和纯文本降级。
- v0.10.0 时间画布：自动测试覆盖月/年日历边界、闰年月末、持续与预警形状、事件类型/类型/标签组合筛选、待排期载荷上限；10 万条同月事项在发布优化构建中的单次投影为约 162 ms，并只返回前 2000 个可见节点。
- v0.8.0–v0.10.0 界面：主列表、时间画布、邮件规则和数据仓库已通过真实业务内容、空状态、编辑态、`760 × 560` 窄窗口、可访问名称与控制台复核；零运行错误、零警告。
- Windows：v0.10.0 x64 NSIS 与便携 ZIP 构建成功；可执行文件文件版本/产品版本均为 0.10.0，当前未配置发布者签名。

## v0.10.1 安装语言补丁验证（2026-09-06）

- 回归复现：加入语言检查后，旧配置因未指定简体中文而失败；补齐后配置检查与生成脚本检查均通过，27 条专用文案齐全，构建使用的文件内容与源码一致。
- 版本一致性、前端类型检查与生产构建通过；前端 42 项测试通过，Rust 格式检查、Clippy 全目标/全 feature 及 79 项测试通过（1 项手工渲染用例默认忽略）；Clippy 仅按项目约定放行本地化 MSVC 的 `linker_messages`。
- 真实安装器以隐藏方式启动，仅操作安装前页面并捕获自身窗口：最终包欢迎页、安装目录页均为中文，底部已替换为“© 2026 闪记”；中间构建的更新选择页也已检查中文。不执行安装/卸载，不启动应用或访问事项正文。
- 当前系统缩放下文字、按钮和目录完整显示，默认焦点可见；生成脚本保留卸载数据复选框默认不勾选的行为。此处不代表完整键盘、多主题或卸载流程已实测。
- Windows 安装包 `shanji-0.10.1-windows-x64-setup.exe` 为 4,295,971 字节；便携 ZIP 为 5,732,465 字节，仅含 `shanji.exe` 与空 `portable.flag`，应用文件/产品版本均为 0.10.1。SHA-256 文件已生成，安装包未签名。
- 手工复验：双击 0.10.1 安装包，检查欢迎页与“下一步/取消”；前进一次，检查更新选择页或安装目录页后退出。完整安装、重装、卸载及异常测试应在可恢复的隔离 Windows 环境进行，不勾选删除真实用户数据。

## v0.10.2 A1 仓库入口补丁（2026-09-07）

- 用户反馈 A1 选择仓库位置无效，其后依赖仓库配置的项目尚无法验收。查到主窗口缺少 `dialog:allow-open`，且选择器调用在异常处理之外，原有浏览器预览绕过原生分支而漏检。
- 新增仅 `main` 窗口可用的目录选择权限；选择器与配置保存纳入同一可恢复路径，取消不写配置，失败可“重新选择文件夹”，忙碌时不重复打开。
- 新增 7 项回归：权限作用域、中文路径确认、取消选择、取消确认、选择器失败后重试、保存失败后重试、重复点击。权限检查在旧配置下先失败，补丁后通过；全量前端 49 项测试通过，类型检查零错误/警告，生产前端构建与版本一致性检查通过。
- 构建生成的权限清单已只读核对，新权限仅授予主窗口；不新增文件系统插件权限、联网行为、数据库迁移或 Rust 业务规则。
- Windows 0.10.2 NSIS 与便携 ZIP 已构建，大小分别为 4,296,787 与 5,732,223 字节；应用文件/产品版本均为 0.10.2，安装语言检查通过，SHA-256 文件已生成。延续未签名测试包状态，未作正式发布。
- 原生对话框使用模拟测试覆盖，不代表已在用户安装环境复测 A1；本次不接管用户已运行的应用、不替用户配置仓库。Windows 新包需先重验 A1，再继续相关 A 项，macOS/Linux 选择器仍需实机复核。
- C 区间和视觉需求只写入 `PRD-acceptance-rework-draft.md` 与设计草案，未修改时间画布代码。

## v0.11.0 区间与工作台返工（2026-09-07）

- 用户确认返工 PRD 后同步第 7.14 节、两轮前端设计与 ADR-0013，再实施区间投影和界面。账户/云空间与同步改为后续独立排期，本次没有云能力、生产依赖、额外权限或数据迁移。
- Rust 以创建至有效截止（没有截止则至当前时间）投影；活动/预警为条内标记。逾期和完成保留原截止；周期沿持久化月日锚点向未来展开，未知过去标注历史不完整。时钟/时区可注入，日视图保留 23/25 小时；回拨日的重复小时与区间说明包含偏移以免混淆。
- 主工作台和甘特图采用紧凑冷白/深蓝计划表；名称列及日期表头固定，虚拟行只渲染视窗与缓冲区，键盘上下/首尾/横向导航可用。更新失败保留旧图与实际日期标题，首次失败不显示为空数据，可重试或回到列表；主工作台不再直接展示底层错误。
- 前端 56 项测试通过，类型检查零错误/警告；覆盖 A1 桌面分支、区间/高亮/周期合行、筛选、请求竞态、月末导航、失败恢复、2000 行键盘移动与组合态保护。
- Rust 全量回归 87 项通过、1 项默认忽略，格式检查与全目标/全 feature Clippy 通过（仅按项目约定放行本地化 MSVC 的 `linker_messages`）；10 万条合成数据在发布优化模式下单次投影从约 568 ms 降到 109 ms。优化只取消返回上限之外的字段格式化，仍校验每条时间并统计全部区间/异常项；不把这次单样本当作完整桌面 P95。
- 浏览器真实渲染使用 `qa/timeline.html` 的隔离示例。已检查中文主列表、月/周视图、浅色/深色、高对比度及减少动态效果样式、760×560 窄窗口、失败后保留旧图与重试恢复、显示已完成。2000 条视窗首屏实际渲染 19 行，End 跳至第 2000 条并保留焦点，表头位置不变；末尾视窗渲染 12 行。周视图截图保存于 `artifacts/shanji-0.11.0-gantt-week.png`。
- 主题验收通过开发夹具激活应用原有媒体规则，不改变操作系统设置，不等同于真实系统强制颜色或原生 WebView 验收。浏览器工具额度中断，强制颜色样式最后一轮未完成；真实中文输入法、三平台字体/滚动、完整桌面 P95 和安装环境 A1 仍待验收。
- 测试页面不进入生产构建；不接管用户正在运行的应用，不执行安装或卸载，不访问真实事项库。验收顺序见 `docs/ACCEPTANCE-0.11.0.md`。
- 最终 Windows x64 NSIS 与便携 ZIP 已重新构建，分别为 4,310,763 与 5,748,298 字节；文件/产品版本均为 0.11.0，版本一致性和生成安装脚本的 27 条中文文案检查通过。便携 ZIP 仅含程序和空 `portable.flag`，其中程序摘要与最终构建一致；ASCII 文件名的安装包与构建源摘要一致，SHA-256 校验文件已生成。包未签名，仍为 `Unreleased` 验收包，未创建正式标签或 GitHub Release。

## v0.11.1 提醒设置去重（2026-09-08）

- 用户确认设置页中的展示渠道、全局频率和提醒中心入口语义重复。已按 PRD 7.7.1 和 `DESIGN.md` 两轮方案拆为“提醒显示”与“错过提醒”。
- 设置页只保留系统持续提醒、屏幕角落提醒窗两个展示开关，并明确两者不改变提醒频率；使用“系统/闪记”来源标签区分出现位置。待确认提醒显示数据库实际数量并直接打开现有列表。
- 删除普通事项全局未确认重复开关及其错误测试入口；旧字段不迁移、不删除，继续兼容读取，但调度不再使用，保存设置时写回关闭。事项下一次提醒完全由自身提醒方案、已保存间隔和安静时段决定。
- 删除提醒中心测试入口，避免为验证入口而创建真实事项；标准通知、系统持续提醒和屏幕角落提醒窗仍保留各自测试路径。本次不增加依赖、权限、联网行为或 schema 迁移。
- 前端 57 项测试、类型检查和生产构建通过；Rust 87 项测试通过、1 项手工渲染用例默认忽略，格式检查和全目标/全 feature Clippy 通过（仅按项目约定放行本地化 MSVC 的 `linker_messages`）。
- 使用真实中文业务数据复核浅色、深色、高对比度、强制颜色样式和入口跳转；复核中发现并修正设置抽屉深色状态卡与按钮对比度，截图为 `artifacts/shanji-0.11.1-reminder-settings.png`。主题通过开发夹具激活实际媒体规则，不等同于原生 WebView 和操作系统实机验收。
- Windows x64 NSIS 与便携 ZIP 已构建，分别为 4,309,399 与 5,748,970 字节；文件/产品版本均为 0.11.1，版本一致性与生成安装脚本中文检查通过。便携 ZIP 仅含程序和空 `portable.flag`，程序摘要与最终发布构建一致；安装包仍未签名，保持 `Unreleased` 验收包状态。
- 桌面系统持续通知形态、置顶窗不抢焦点及托盘数量仍需安装环境统一验收，步骤见 `docs/ACCEPTANCE-0.11.1.md`。

## v0.12.0 签名更新与中文字体（2026-09-08）

- 用户确认 PRD 后同步第 7.16 节、两轮前端设计与 ADR-0014。自动检查默认关闭；启用后启动延迟 30 秒、每 24 小时最多检查一次，手动检查不受此限制。更新状态保存于 schema v11 的设备本地表，不进入事项数据包或仓库归并。
- 更新只读取官方仓库稳定版 `latest.json`，客户端同时校验官方 Release 下载来源和 Tauri 更新签名；篡改来源、清单、更新包或签名时拒绝安装，当前程序与本机数据保持不变。下载、安装和重启均由用户主动触发，并提供取消、24 小时后提醒及失败恢复入口。
- 更新前后共用 `src/release-notes/v0.12.0.md` 的 5 条中文说明。首次升级到新版本后只显示一次；全新安装不会误报历史更新。托盘、设置页和主工作台共用同一检查状态，系统通知每个版本最多一次。
- Windows NSIS/macOS 应用包/Linux AppImage 走应用内更新；Windows 便携版和 Linux DEB 等不能安全原地替换的形态只打开官方发布页。macOS 签名公证、Linux AppImage 原地替换及 Windows 从 v0.12.0 到后续版本的完整闭环仍需真实发布验收。
- 中文界面改用微软雅黑 UI、苹方、Noto Sans CJK 与系统无衬线回退；正文基线 14px，常规辅助文字不小于 12px。主工作台、设置页、授权条和更新说明抽屉已用真实中文业务数据复核，当前版本说明采用单一校验节点，不重复绘制升级轨道。
- 前端类型检查零错误/警告，16 个测试文件共 65 项通过，生产构建与版本一致性检查通过；Rust 90 项通过、1 项手工渲染用例默认忽略，格式检查和全目标/全 feature Clippy 通过。
- Windows x64 NSIS 实际构建成功，安装/卸载脚本的 27 条中文文案检查通过；安装包为 4,844,019 字节，同时生成 416 字节的更新签名。便携 ZIP 为 6,520,284 字节，只含 `shanji.exe` 与 `portable.flag`，文件/产品版本均为 0.12.0。
- 更新私钥已生成在仓库外的维护者目录，未写入源码或产物；正式发布前仍需由维护者将其配置为 GitHub Actions Secret `TAURI_SIGNING_PRIVATE_KEY`。Windows Authenticode 发布者签名是另一项发布门槛，本次更新签名不会消除 SmartScreen 的“未知发布者”提示。
- 手工验收步骤见 `docs/ACCEPTANCE-0.12.0.md`；当前保持 `Unreleased`，未创建标签或 GitHub Release。

## v0.13.1 状态甘特图、个人仓库与角落提醒（2026-09-08）

- 按已确认 PRD 7.17 和 `DESIGN.md` 两轮设计实施“缝合式状态条”。Rust 返回阶段区间、原截止和完成刻点；未完成逾期从截止延伸到注入时钟的当前时间，提前完成与逾期完成使用不同分段，不推断旧数据缺失的完成时间。
- 个人仓库在选择位置后立即通过临时文件校验并原子写入本机 `.sjpack`。汇入仍需预览和确认；事务提交后刷新事项、类型、标签、待选择事件和待确认提醒，托盘数量由后端同步更新。归并成功但快照写出失败时分别报告并提供“重新写入”。
- 角落提醒窗改为先成功隐藏再清空队列；隐藏失败保留事项、关闭按钮和业务操作。关闭按钮脱离拖动命中区，正常与空载荷均存在，支持 `Esc` 并忽略 IME 组合态。系统关闭请求成功后发送同一隐藏事件，关闭不会完成、暂停、确认或稍后提醒。
- 浏览器以真实中文事项复核主甘特图、仓库入口、正常提醒与空载荷关闭路径；两个页面均无运行错误或警告。状态段同时使用颜色、纹理、截止刻痕和文字；本次不新增动画、数据库迁移、生产依赖、权限或联网行为。
- 前端类型检查零错误/警告，17 个测试文件共 71 项通过，生产构建成功；Rust 92 项通过、1 项手工 Excel 渲染用例默认忽略，格式检查和全目标/全 feature Clippy 通过。
- Windows x64 NSIS 与便携 ZIP 已构建，分别为 4,848,160 与 6,529,072 字节；更新签名 416 字节，生成安装脚本的 27 条中文文案检查通过，文件/产品版本均为 0.13.1。便携 ZIP 只含 `shanji.exe` 与 `portable.flag`。
- 验收顺序见 `docs/ACCEPTANCE-0.13.1.md`。本节随 v0.13.1 发布；macOS/Linux 实机与从 v0.12.0 通过正式 Release 升级到 v0.13.1 仍需在远端附件生成后复核。

## 尚未完成

- v0.10.1 安装语言补丁：完整安装成功、相同版本重装、卸载确认/完成、运行组件故障恢复，以及深色/高对比度与更多 DPI 的真实交互仍待统一验收；本次检查不执行用户机器上的实际安装或卸载。
- macOS/Linux 真实设备的快捷键、托盘、通知、休眠恢复和分发验证。
- 系统通知权限的完整引导与拒绝后的恢复入口。
- 全文搜索、批量操作、完整 JSON 导入导出。
- 系统休眠/时区变化的事件驱动监听；当前由持久计划和 30 秒低频健康检查恢复。
- Windows Authenticode 发布者签名、macOS 签名与公证。
- v0.12.0 到后续稳定版的 GitHub Release 发现、签名下载、安装、重启和更新后说明完整实测；需要先配置发布私钥 Secret 并发布下一版本。
- v0.7.1 的 Windows NSIS 覆盖更新/重装，以及 macOS/Linux 对应安装包数据目录仍需真实设备验收。
- macOS/Linux 上个人仓库断开恢复、系统凭据存储、实际 SMTP 投递和时间画布字体/滚动仍需真实设备验收。
- 性能基线设备上的连续 P95 自动采样；10 万条时间画布单次压力回归已建立。
