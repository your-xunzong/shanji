[package]
name = "shanji"
version = "0.5.0"
description = "离线优先的快速备忘与提醒工具"
authors = ["Shanji contributors"]
license = "MIT"
repository = ""
edition = "2024"
rust-version = "1.85"

[lib]
name = "shanji_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
keyring = { version = "3.6.3", features = ["apple-native", "windows-native", "linux-native-sync-persistent", "crypto-rust"] }
lettre = { version = "0.11.23", default-features = false, features = ["builder", "smtp-transport", "rustls", "ring", "rustls-platform-verifier"] }
rusqlite = { version = "0.37", features = ["bundled"] }
rust_xlsxwriter = "0.96.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-autostart = "2"
tauri-plugin-dialog = "2"
tauri-plugin-global-shortcut = "2"
tauri-plugin-notification = "2"
tauri-plugin-single-instance = "2"
thiserror = "2"
uuid = { version = "1", features = ["v4"] }

[target.'cfg(windows)'.dependencies]
tauri-winrt-notification = "0.7.3"
winreg = "0.55"

[dev-dependencies]
tempfile = "3"
